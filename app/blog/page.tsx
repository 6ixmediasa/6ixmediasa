import type { Metadata } from 'next';
import Image from 'next/image';
import Link from 'next/link';
import CTA from '@/components/CTA';
import { posts } from '@/lib/content';

export const metadata:Metadata={title:'Blog | 6ixMedia SA',description:'Practical guides, ideas and updates from 6ixMedia SA.',alternates:{canonical:'/blog'}};
export default function BlogPage(){return <><section className="on-dark bg-ink"><div className="shell py-16 lg:py-24"><p className="eyebrow-dark">Our work</p><h1 className="mt-4 font-display text-[42px] font-medium tracking-[-0.03em] text-white lg:text-[54px]">Blog</h1><p className="mt-5 max-w-2xl text-[17px] text-ink-dim">Practical guides, business ideas, tools and lessons from the work we do.</p></div></section><section className="shell py-12 lg:py-16">{posts.length===0?<div className="rounded-2xl border border-dashed border-paper-edge py-20 text-center"><h2 className="font-display text-[22px]">No posts published yet.</h2></div>:<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{posts.map(p=><Link key={p.slug} href={`/blog/${p.slug}`} className="group overflow-hidden rounded-2xl border border-paper-edge bg-white transition hover:-translate-y-1 hover:border-azure-400">{p.coverImage&&<div className="relative aspect-[16/9]"><Image src={p.coverImage} alt={p.title} fill className="object-cover" sizes="(max-width:768px) 100vw, 33vw"/></div>}<div className="p-6"><p className="text-[12px] uppercase tracking-[.12em] text-azure-600">{p.publishedAt?new Date(p.publishedAt).toLocaleDateString('en-ZA',{year:'numeric',month:'short',day:'numeric'}):'6ixMedia SA'}</p><h2 className="mt-2 font-display text-[22px] tracking-[-.02em]">{p.title}</h2><p className="mt-3 text-[14px] leading-relaxed text-ink/70">{p.excerpt}</p></div></Link>)}</div>}</section><CTA/></>}
